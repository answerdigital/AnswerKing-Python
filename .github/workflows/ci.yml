name: CI

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  test:

    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_ROOT_PASSWORD: UnsafePassword098
          MYSQL_DATABASE: answerking_app
        ports: ['3306:3306']

    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
        
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install poetry
        poetry update
        
    - name: Run Migrations
      run: poetry run python manage.py migrate
      env:
        DATABASE_NAME: answerking_app
        DATABASE_USER: root
        DATABASE_PASS: UnsafePassword098
        DATABASE_HOST: 127.0.0.1
        DATABASE_PORT: 3306
        SECRET_KEY: django-insecure-x977=v5a2q-e%_5$b9ge@jzk%s_nhk2l8_use&h@*m$w33dopr
        
    - name: Run Tests     
      run: |
        poetry run python manage.py test
        poetry run black . --line-length=79
        poetry run pycodestyle . --ignore=E501
        poetry run pyright
      env:
        DATABASE_NAME: answerking_app
        DATABASE_USER: root
        DATABASE_PASS: UnsafePassword098
        DATABASE_HOST: 127.0.0.1
        DATABASE_PORT: 3306
        SECRET_KEY: django-insecure-x977=v5a2q-e%_5$b9ge@jzk%s_nhk2l8_use&h@*m$w33dopr
