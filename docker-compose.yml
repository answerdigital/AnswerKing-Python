version: '3.3'
services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'answerking_app'
      MYSQL_ROOT_PASSWORD: 'Admin123'
    ports:
      - '3306:3306'
    expose:
      - '3306'
